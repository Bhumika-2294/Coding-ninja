#include <bits/stdc++.h> 
#include <algorithm>
using namespace std;
int sumOfMaxMin(int arr[], int n){
	// Write your code here.
	sort(arr, arr + n);
	int max=arr[n-1];
	int min=arr[0];
	return min + max ;
}
